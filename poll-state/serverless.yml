service: poll-state

frameworkVersion: ">=1.28.0 <2.0.0"

provider:
  name: aws
  runtime: go1.x
  stage: dev
  region: eu-central-1
  tags:
    project: sls-demo-aboczek
    owner: aboczek
  iamRoleStatements:
    - Effect: Allow
      Action:
        - s3:PutObject
        - s3:GetObject
      Resource: arn:aws:s3:::dev-aboczek-serverless-site/*

package:
 exclude:
   - ./**
 include:
   - ./bin/**

functions:
  read:
    handler: bin/read
    events:
      - http:
          path: state
          method: get
  update:
    handler: bin/update
    events:
      - http:
          path: state
          method: put
